def render_watchtower():
    if st.button("Back", key="back_watchtower"):
        st.session_state.location = "village_square"
        st.rerun()

    inner = """
    <p class="world-title">🏰 Confidence Calibration</p>
    <p class="location-sub">Match belief strength with reality.</p>
    """

    st.markdown(cinematic_scene("grade-tower", inner, "min(42vh,420px)"), unsafe_allow_html=True)

    score = st.session_state.get("trust_score", 50)

    st.markdown(f"""
    <div style="
    max-width:1100px;
    margin:2rem auto;
    padding:3rem;
    border-radius:36px;
    background:rgba(255,255,255,.04);
    border:1px solid rgba(255,255,255,.10);
    backdrop-filter:blur(40px);
    box-shadow:0 30px 100px rgba(0,0,0,.35);
    ">

    <div style="letter-spacing:.2em;color:#E8C97D;text-transform:uppercase;font-size:.8rem;">
    Calibration Analytics
    </div>

    <h1 style="color:#F5ECD7;font-size:5rem;margin:.5rem 0;">
    {score}%
    </h1>

    <p style="color:rgba(245,236,215,.7);">
    Confidence Alignment Score
    </p>

    </div>
    """, unsafe_allow_html=True)

    render_mentor_panel("golem")
